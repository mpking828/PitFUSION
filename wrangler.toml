name = "pitfusion"
pages_build_output_dir = "public"
compatibility_date = "2026-09-01"

# Secrets are NOT defined here. Set them in the Cloudflare Pages project
# (Settings -> Environment variables, encrypted, for Production and Preview):
#   NEXUS_API_KEY, TBA_API_KEY, YOUTUBE_API_KEY
# For local `wrangler pages dev`, put them in a gitignored .dev.vars file
# (see .dev.vars.example).
